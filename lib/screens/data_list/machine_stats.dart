
import 'package:flutter/material.dart';

class MachineStatsScreen extends StatelessWidget {
  const MachineStatsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('機種別集計画面'));
  }
}
