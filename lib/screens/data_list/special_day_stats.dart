import 'package:flutter/material.dart';

class SpecialDayStatsScreen extends StatelessWidget {
  const SpecialDayStatsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('ゾロ目・特定日別集計画面', style: TextStyle(fontSize: 20)),
    );
  }
}
