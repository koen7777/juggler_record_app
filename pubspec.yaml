name: juggler_record_app
description: こえん式ジャグラー収支表 Web版
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.5
  shared_preferences: ^2.1.0
  flutter_localizations:
    sdk: flutter
  intl: ^0.20.2      # ← Flutter SDK に合わせた最新版
  table_calendar: ^3.0.9

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
